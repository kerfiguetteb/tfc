      {% for picture in post.pictures %}
        <div class="col" style="width: 100px; flex: none; margin: 5px;">
          <img src="{{ vich_uploader_asset(picture, 'imageFile') | imagine_filter('thumb') }}" width="100" alt="">
          <form method="post" action="{{ path('admin.picture.delete', {id: picture.id}) }}" style="display: inline-block" onsubmit="return confirm('Êtes vous vraiment sûr ?')">
            <input type="hidden" name="_method" value="DELETE">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
            <button class="btn btn-danger">Supprimer</button>
          </form>
        </div>
      {% endfor %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
  <div class="col-md-4">
    <div class="row flex justify-content-start flex-wrap">
      {# {% for picture in post.pictures %}
        <div class="col" style="width: 100px; flex: none; margin: 5px;">
          <img src="{{ vich_uploader_asset(picture, 'imageFile') | imagine_filter('thumb') }}" width="100" alt="">
          <form method="post" action="{{ path('admin.picture.delete', {id: picture.id}) }}" style="display: inline-block" onsubmit="return confirm('Êtes vous vraiment sûr ?')">
            <input type="hidden" name="_method" value="DELETE">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
            <button class="btn btn-danger">Supprimer</button>
          </form>
        </div>
      {% endfor %} #}
    </div>
  </div>

{{ form_end(form) }}

          {# <a href="{{ path('admin.picture.delete', {id: picture.id}) }}" class="btn btn-secondary">Editer</a>
          <form method="post" action="{{ path('admin.picture.delete', {id: picture.id}) }}" style="display: inline-block" onsubmit="return confirm('Êtes vous vraiment sûr ?')">
            <input type="hidden" name="_method" value="DELETE">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
            <button class="btn btn-danger">Supprimer</button>
          </form> #}
